import 'exporter.dart';

const screenWidthHeight = double.infinity;
const sideBarWidth = 180.0;

const size2 = 2.0;
const size4 = 4.0;
const size8 = 8.0;
const size16 = 16.0;

const padding2 = EdgeInsets.all(size2);
const padding4 = EdgeInsets.all(size4);
const padding8 = EdgeInsets.all(size8);
const padding16 = EdgeInsets.all(size16);

const headerColor = Colors.blue;
const selectedFoodColor = Colors.green;

const headerFontSize = 32.0;
const itemsFontSize = 20.0;
